exports.roles = 'user';

exports.description = "Displays your profile or the profile for a specified user.";

exports.invoke = function(shell) {
    shell.warn("The profile command is unfinished. When complete it will display your own profile information or the profile information for a specified user.");
};